version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations/"
    queries: "queries/"
    gen:
      go:
        package: "storage"
        out: "storage"
        emit_interface: true
        emit_json_tags: true
